去 MySQL 的 Real 表里，捞出过去 72 小时的 5 个字段（空温、空湿、土温、土湿、EC），拼成一个 72 行 × 5 列 的数组。交给 x1_scaler.pkl 压缩。
去调“和风天气 API”，拿到明天 24 小时的 2 个预报字段（温度、相对湿度），拼成一个 24 行 × 2 列 的数组。交给 x2_scaler.pkl 压缩。
把这两个压缩后的数组丢给 best_model.pth，模型瞬间吐出一个 24 行 × 3 列 的小数矩阵。
把这个矩阵交给 y_scaler.pkl 还原,最后通过fastapi后端存储到数据库，需要创建一个数据库表为predictions（这个表存储预测的数据）